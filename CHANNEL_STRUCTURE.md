# Структура и права Discord Shadow Squad

## Матрица доступа

| Канал | РЛ | Знаменосец | Сержант | Летописец | Друзья | Гости / остальные |
| --- | --- | --- | --- | --- | --- | --- |
| Остальные текстовые | видеть | видеть | видеть | видеть | видеть | видеть |
| 🐓флудильня | да | да | да | да | да | нет |
| 🐷отсутствия-и-опоздания (ID `1200807297111306280`) | да | да | да | да | да | нет |
| ⚔️РТ・Спец-отряд | да | да | да | да | да | нет |
| 🤪Душевнобольная・офицеры | да | да | нет | нет | нет | нет |
| 🔑Создать комнату・Ключ | без изменений | без изменений | без изменений | без изменений | без изменений | без изменений |

Права меняются скриптом `deploy/configure_channel_permissions.py`. Без параметров
он только показывает безопасный план для четырёх специальных каналов. Параметр
`--apply` применяет его к серверу. Массовое открытие остальных текстовых каналов
включается отдельно параметром `--include-public-text`. Скрипт меняет только
права «Просмотр канала» и «Подключаться», остальные разрешения сохраняются.

## Рекомендуемая упрощённая структура

1. **ИНФОРМАЦИЯ** — `👋начать-здесь`, `📯объявления`, `📰новости`.
2. **ГИЛЬДИЯ** — `🐓флудильня` и `😆мемесы`.
3. **СТАТИК** — `🐷отсутствия-и-опоздания`, `🎁недельный-лут`, читаемая
   лента Raider.IO `🚧прогресс-гильдии`, ссылки `📈warcraft-logs`, закрытый
   `💚чат-хилов`, читаемый `🏠домик-ебли・разборы`, форум `🌙midnight・тактики`,
   `📊настройка-archon`, `🧭настройка-wowutils` и `✨weak-auras`.
   Archon и WeakAuras видны всем. Midnight видят только РЛ, Знаменосец и
   Сержант.
   WowUtils видят только участники основного состава: РЛ, Знаменосец и Сержант.
4. **ГОЛОСОВЫЕ** — `🔑Создать комнату・Ключ`, `⚔️РТ・Спец-отряд`,
   `🤪Душевнобольная・офицеры`, `🎙️Собеседование`, `🐼АФК`.
5. **СЛУЖЕБНЫЕ** — каналы ботов и интеграций, включая технические ленты
   `📺стримы` и журнал приглашений `🔗invite-log`. Объединить
   активный `🤖bot-console` и канал обсуждения `💬бот-и-предложения`.

Архивные каналы удалены после явного подтверждения владельца сервера. Отдельная
категория «Архив» не нужна.
